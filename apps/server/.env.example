# KAS Racing Server Configuration
# Copy this file to .env and fill in the values
# IMPORTANT: Never commit .env file - it contains secrets!

# Network: "mainnet" or "testnet"
NETWORK=testnet

# Server port (default: 8787, Fly.io uses 8080)
PORT=8787

# Treasury wallet private key (64 hex characters)
# Used for sending reward payouts
# Generate a new key for each environment!
TREASURY_PRIVATE_KEY=

# Treasury change address (receives leftover from reward transactions)
# Must be a valid Kaspa address for the selected network
TREASURY_CHANGE_ADDRESS=

# Oracle private key (64 hex characters)
# Used for signing duel settlement transactions
# Generate a new key for each environment!
ORACLE_PRIVATE_KEY=

# Minimum reward amount in KAS (default: 0.02)
MIN_REWARD_KAS=0.02

# CORS origin (comma-separated list of allowed origins)
# Example: https://kas-racing.vercel.app,https://example.com
# Leave empty for permissive dev mode
CORS_ORIGIN=

# Skip key validation (for development only - reward payouts will not work)
# SKIP_KEY_VALIDATION=true

# --- Fly.io Deployment ---
# Set secrets using: fly secrets set KEY=value
#
# Required secrets:
#   fly secrets set NETWORK=mainnet
#   fly secrets set TREASURY_PRIVATE_KEY=your_64_hex_chars
#   fly secrets set TREASURY_CHANGE_ADDRESS=kaspa:qz...
#   fly secrets set ORACLE_PRIVATE_KEY=your_64_hex_chars
#   fly secrets set CORS_ORIGIN=https://kas-racing.vercel.app
