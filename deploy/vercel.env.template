# KAS Racing — Vercel Environment Variables (KASPLEX Testnet)
# Copy values into Vercel -> Project -> Settings -> Environment Variables

# API server URL (Railway domain)
VITE_API_URL=https://REPLACE_WITH_RAILWAY_API_DOMAIN

# Block explorer URL (used by Proof page and HUD links)
VITE_EXPLORER_URL=https://zkevm.kasplex.org
